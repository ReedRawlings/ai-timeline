{
  "name": "ai-timeline",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-events.js && vite",
    "build": "node scripts/build-events.js && node scripts/build-stock-data.js && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lightweight-charts": "^4.2.2"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "vite": "^6.0.0"
  }
}
