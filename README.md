# AI Timeline (Hugo Static Site)

## How to Use
1. Upload this to a new GitHub repo
2. Push to the `main` branch
3. GitHub Actions will build and deploy it to GitHub Pages
4. Optional: connect to Netlify to use the CMS interface