# Decision Log

Append-only. Format: `date | decision | reasoning`. New decisions go at the bottom. Never edit or delete existing rows — if a decision is reversed, append the reversal as a new row.

| Date | Decision | Reasoning |
|---|---|---|
| ~2026-06-07 | Studies treated as discourse objects, not ground truth | Most ran on obsolete models; conditions get stripped in circulation |
| ~2026-06-22 | Chat context doesn't transfer between tools; handoff docs are the mechanism | — |
| 2026-06-28 | The most valuable artifacts are maintained data assets, not one-time writeups | Citations rot; maintained tables don't |
| 2026-06-28 | Build sequence: stat provenance register first (cheap, disciplines everything), then layoffs + environmental ledger as flagship tables | Register is the meta-asset that makes the others trustworthy |
| 2026-07-01 | Media coverage (NewsAPI.ai) runs as a separate "Media Narrative Index," never blended into any backlash composite; GDELT deferred | Media volume measures editorial/PR decisions, not public sentiment; per-theme directional biases identified |
| 2026-07-01 | GDELT usable only as raw corpus/volume signal, not pre-coded sentiment or events | Dictionary-based tone scoring, CAMEO taxonomy mismatch, syndication duplication |
| 2026-07-03 | All assets are public | — |
| 2026-07-03 | Public ≠ polished; don't overindex on polish | — |
| 2026-07-03 | Building our own layoffs dataset rather than relying on Challenger/layoffs.fyi | Existing trackers are US/tech-centric, press-derived, and structurally undercount India/Global South |
| 2026-07-03 | WIP cap: two active builds (layoffs dataset + provenance register); everything else parked with re-entry points | Too many spec'd-not-built threads; losing track |
| 2026-07-03 | Single repo (`ai-timeline`) becomes the home for the whole project: datasets, docs, decision log, site | Consolidation away from scattered chats; repo is the shared context across machines/tools |
| 2026-07-03 | Datasets live as standalone files with their own schemas; the timeline references dataset rows, never the reverse | Keeping layoff data inside events.yaml inherits the timeline's significance rubric and rebuilds Challenger's selection bias |
| 2026-07-03 | Refactor the existing `layoffs:` blocks out of events.yaml into the standalone dataset | Nested blocks flatten attribution and cap coverage at timeline-worthiness |
| 2026-07-03 | Assets organized into five themes (labor, data centers, sentiment & discourse, claims & verification, cultural record); some assets are sub-components, not standalone | Flat list of ~12 assets was untrackable |
| 2026-07-03 | Data center project-level outcomes is a sub-table of the buildout tracker, same asset class (projected project → outcome) | Same underlying entities; joining them makes announced-vs-built visible |
| 2026-07-03 | Provenance register follows the fact-check-site model: per-claim research, stable permalink-able row IDs | Register is meant to be citable claim-by-claim |
| 2026-07-03 | Environmental disclosure ledger reparented under claims & verification as a shelf of the register | Same conditions-preserving structure |
| 2026-07-03 | BLS added as the employment-actuals source for the labor theme | Press-independent realized-headcount layer completing predictions vs. announcements vs. actuals |
| 2026-07-03 | Update-grief event log is explicitly low priority | Unclear measurement approach for the reaction layer; events-only core noted for later |
| 2026-07-03 | Provenance register stored as CSV (`data/provenance/register.csv`), not YAML | Tabular, one-row-per-claim data; matches the layoffs dataset convention (CSV); YAML stays for timeline events. Flagged for human veto — see docs/master-status.md |
| 2026-07-03 | Vercel deploys skipped for commits touching only `docs/`, `data/provenance/`, `data/layoffs/` via `ignoreCommand` in vercel.json | Docs/dataset commits don't change the built site; verified against current Vercel docs (exit 0 = skip, exit 1 = build) |
| 2026-07-03 | Layoffs dataset scope: AI-attributed layoffs only; non-AI-attributed layoffs are out | For share-of-job-loss comparisons, external monthly/quarterly job-loss reports (e.g. Challenger) serve as the denominator instead of tracking non-AI rows ourselves |
| 2026-07-03 | No layoffs work exists outside this repo; the handoff's reconciliation step is moot | Human confirmed directly |
| 2026-07-03 | Layoffs dataset format: CSV (`data/layoffs/layoffs.csv`) | Flat tabular rows; YAML adds nothing here |
| 2026-07-03 | events.yaml is the source of truth for the migration count: 32 `layoffs:` blocks, not the handoff's ~16 estimate | The handoff's number was an estimate from memory; the repo is the source data |
| 2026-07-03 | Layoffs schema approved; migration executed (32 rows, all `needs_recode`); `layoffs:` blocks retired from events.yaml in favor of `layoff_ids` references | Every migrated row needs a human-reviewed attribution recode with a verbatim quote before it counts as coded data |
| 2026-07-03 | Attribution recode pass completed on all 32 migrated rows; 11 removed for having no AI attribution from anyone (Amazon 1/23, EA 3/23 & 2/24, Unity, UPS, Tesla, Intel 8/24 & 7/25, Amazon 10/24, Google 8/25, Meta 10/25) | Scope is AI-attributed only; the legacy labels were narrative drift — primary memos fetched and contain no AI causation |
| 2026-07-03 | Boundary precedents: AI-attributed attrition/freezes (Klarna) and projections (IBM 2023) stay in the dataset with count=undisclosed | Attribution is real and explicit in both; the count field must not launder a non-layoff number into the layoff chart |
| 2026-07-03 | Monthly AI-layoffs sweep skill created (`find-ai-layoffs-monthly`) with headline-vs-memo verification discipline | 12 of 32 migrated rows failed recode because press framing had overwritten primary language; the skill encodes those rejection patterns |
| 2026-07-03 | Layoffs rows tagged with `causal_link` (sole/mixed/contextual) and `count_scope` (global/country/unknown); sector populated | Modeled on jobloss.ai's Explicit/Reported/Mixed evidence levels, split into two axes (who said it × how causal) since our ai_attribution already covers who; count_scope marks global vs single-country jobs lost because the dataset is global, not US-only like jobloss.ai |
| 2026-07-04 | Prediction accountability tracker added to the parked list (Theme 4): influencer/poster AI predictions tracked to resolution, submission page, Wayback/Internet Archive snapshot at entry | Forward-looking twin of the provenance register; parked behind the WIP cap pending a short PRD (inclusion criteria, moderation flow) |
| 2026-07-04 | Prediction tracker scope: all AI-related predictions, no skewed selection — optimist and pessimist alike; includes LLM/model-capability, data center/infrastructure, and economic-impact predictions | Neutral inclusion is what makes it a reference instead of a dunk list; mirrors the data-center tracker's for-and-against co-equal stance |
| 2026-07-04 | Timeline backfilled for April 2026 and the March 20–31 gap (events.yaml → 288 events; layoffs.csv +4 rows: Snap, Meta, Cognizant, Oracle) | Timeline had drifted; latest event was 2026-03-20. Ran the criteria-first sweep (curated sources first) plus adversarial verification |
| 2026-07-04 | Model releases are never `major` on their own — a routine/point release, even a frontier flagship, is `notable`/`minor` by default; only a withheld/safety-held model, an open-source reversal, or a genuinely field-reordering paradigm shift makes a model event `major` | The §2 "paradigm model release" wording was over-triggering (flagged DeepSeek V4, GPT-5.5 as major-candidates); "a big lab shipped" is not significance. Encoded in event-conventions §2 |
| 2026-07-04 | Voluntary buyout / early-retirement programs with no company AI attribution are OUT of the layoffs dataset | Microsoft's April 2026 buyout: company framed it as a retirement benefit, no exec tied it to AI, and the circulating Nadella AI quote was actually from the July 2025 layoffs (quote drift). Not an AI-attributed layoff |
| 2026-07-04 | Analyst/press headcount estimates never become the `count`; count stays undisclosed unless the company discloses a figure | Oracle (Mar 31, "up to 30,000" was a TD Cowen estimate; company confirmed only ~21,000 for the full fiscal year, a different scope) and Cognizant (7,000–15,000 was analyst math off the severance budget; company disclosed no headcount) — reaffirms the no-guess rule for the count field |
| 2026-07-04 | Event skills gain an explicit verification step (event-conventions §9) mirroring the layoffs sweep's headline-vs-primary discipline; self-check renumbered to §10 | The backfill's first pass let through a hallucinated "Gemini 3.1 Ultra," a vetoed-not-enacted Maine data-center ban, and a mis-dated Apple/Grok threat — the event skills had no anti-hallucination/verify step, only the structural self-check |
| 2026-07-04 | `find-ai-events-range` skill created for month/range backfills, mirroring `find-ai-layoffs-monthly` | The `find-ai-events-broad`/`-criteria` skills are scoped to "the previous day"; a whole-month/gap backfill had no matching skill and had to repurpose the daily ones |
| 2026-07-05 | Curated sources (§6) hardened into a **reported gate** — the sweep must run them first and list which it hit in the review | Two May-pass attempts skipped §6 for generic "AI news <month>" queries; both missed the Pope Leo XIV AI encyclical (a major cultural moment). Silently skipping the sources kept recurring; making it a reported step is the fix |
| 2026-07-05 | The correction for default-search bias is to **prioritize the curated source list**, NOT to mandate explicit per-category sweeps | Considered a §6b "category-coverage floor" (a checklist of under-served categories to sweep) and rejected it: the curated editors already weight for significance across the whole field, so leading with them fixes the skew without bolting on a taxonomy. Generic "AI news <month>" queries over-return the commercial triad (models/funding/layoffs) and should only fill gaps and verify, never drive the sweep |
| 2026-07-05 | Added the labs' own research feeds (OpenAI/Anthropic/DeepMind) to §6; hardened Superintelligence guidance to the month archive, not the homepage | The May pass missed OpenAI's May 20 disproof of an 80-year-old Erdős discrete-geometry conjecture (first autonomous AI solution of a central open math problem; Gowers: "a milestone in AI mathematics") even though Superintelligence had covered it — the failure was reading only its homepage and reporting the source as "checked." Under-mining a curated source counts as skipping it; the lab feeds are a redundant net for research/science milestones the editors under-cover |
| 2026-07-06 | Claims repo: a claim needs one head claimant when a real one exists (e.g. `REF-LLM-NOVEL-DISCOVERY` → Dario Amodei, "Machines of Loving Grace"); verified-but-different-claimant evidence (DeepMind FunSearch/AlphaEvolve, OpenAI's Erdős disproof) moves to the referent's `authoritative_statement`/`references`, not a competing claim row | Multiple named people/orgs saying adjacent-but-distinct things under one referent made "who's the proponent" ambiguous; picking the clearest general/forward assertion as the head keeps the claim gradable and citable |
| 2026-07-06 | Claims repo: when no individual clears the bar of a firm, general assertion, generalize the claimant instead of forcing a head (`REF-LLM-CONSCIOUSNESS` → "Various AI researchers and lab executives (no single firm proponent identified)"); Sutskever (hedged), Fish/Anthropic (probability estimate), and Lemoine (unhedged but narrow/discredited) all move to the referent's `references` | None of the three individually asserts "LLMs are conscious" as a firm claim; forcing one to be "the" proponent would overstate their position. How to *display* a generalized-claimant claim vs. a single-claimant one is unresolved — see `docs/prds/claims-proponent-display-prd.md` |
| 2026-07-06 | Removed duplicate "OpenAI disproves Erdős conjecture" entry from `events.yaml` (kept L2922, the fuller version with `tier: major`, `Model` tag, `key_figures: Tim Gowers`) | Git blame showed both entries were inserted in the same commit (`9758c4d`) at the same timestamp — one commit accidentally wrote the event twice with slightly different wording, not two independent passes; no separate provenance to preserve |
| 2026-07-05 | Docs swim lanes settled: master-status = registry of every artifact with a state (Parked / Development / Deployed + Maintenance cadence: Daily/Weekly/Monthly/None); parked.md = detail stanzas for Parked assets only; decisions.md = append-only what-we-did-and-why working memory | Three docs were tracking overlapping state and drifting (register shown pending after verification, recode shown pending after completion, layoffs state diverged across two workstream sections); one home per fact fixes it while keeping parked.md as its own browsing surface |
| 2026-07-05 | Parked-asset format: description + why-parked + re-entry condition (+ PRD pointer if one exists); PRD is the ticket OUT of parked, not a requirement to park | Writing PRDs for ideas that may never activate recreates the spec'd-not-built problem the WIP cap exists to kill |
| 2026-07-05 | End-of-session review process: every working session ends by reviewing master-status and decisions.md for updates; human prompts for it | Manual push Reed takes on; keeps the registry from drifting between sessions |
| 2026-07-05 | AI Claims Repository (docs/Claims: referents + claim variants, accuracy-graded) registered, state Development; direction: provenance register and Claims Repository will essentially merge — provenance rows are claims to be tracked/categorized long-term | Provenance is statements made about AI (environmental impact etc.); those are claims; the register's deep-research treatment becomes the standard for high-traffic claims. Merge mechanics/timing open |
| 2026-07-05 | CLAUDE.md carries the session workflow: session start = read master-status (open items + maintenance cadences are the backlog) and check decisions.md before re-litigating; session end = review both and record changes | Fresh sessions read CLAUDE.md first; it now routes them to the registry instead of assuming context |
| 2026-07-05 | Provenance register merged into the AI Claims Repository; data/provenance/, validate-provenance.py, and propose-provenance-entry retired; propose-claim-entry skill created | Provenance rows are claims (decided earlier today); executed. ID map: water-500ml-per-prompt -> REF-REN-WATER enriched + C028b (mostly false); gemini-energy-per-prompt -> REF-GOOGLE-GEMINI-FOOTPRINT + C044/C044b; openai-energy-per-query -> REF-OPENAI-PERQUERY + C045/C045b (premature/unverifiable); ai-layoffs-challenger-counts -> REF-CHALLENGER-AI-CUTS + C046/C046b (circulating form mixed/true-but-misleading). All research content preserved in referent statements/notes and accuracy rationales |
