{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ':!docs' ':!data/layoffs'",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" }
  ]
}
